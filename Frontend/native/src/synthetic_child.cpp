// Workstream A: compiled controlled synthetic child (replaces lodge/synthetic_child.py
// for native synthetic sessions). Developer fixture only; no engine or content loader.
int main() { return 2; }
